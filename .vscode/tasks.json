{
    "tasks": [
        {
            "type": "cppbuild", 
            "label": "C/C++: gcc.exe build active file", 
            "command": "C:\\msys64\\ucrt64\\bin\\gcc.exe", 
            "args": [
                "-fdiagnostics-color=always", // Enables colored diagnostic messages.
                "-g", // Include debugging information in the output.
                "${file}", // The active file to be compiled.
                "-o", // Specifies the output executable file.
                "${fileDirname}\\${fileBasenameNoExtension}.exe" // Output executable name.
            ], 
            "options": {
                "cwd": "${fileDirname}" // Sets the working directory to the file's directory.
            }, 
            "problemMatcher": [
                "$gcc" // Use GCC-specific problem matchers for error detection.
            ], 
            "group": {
                "kind": "build", // Marks this task as a build task.
                "isDefault": true // Sets this task as the default build task.
            }, 
            "detail": "Task generated by Debugger." // A description for this task.
        }
    ],
    "version": "2.0.0"
}
